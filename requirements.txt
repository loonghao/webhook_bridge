# Core dependencies for webhook-bridge Python executor
# This file is used for CI/CD and basic installation
# For development dependencies, use pyproject.toml with Poetry

# Core runtime dependencies (matching pyproject.toml)
pyyaml>=6.0
grpcio>=1.50.0
grpcio-tools>=1.50.0
